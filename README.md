# Avoided-Emissions-Portfolio-optimization
Extends an SSRN paper on Implied Temperature Rise (ITR) portfolio alignment by embedding avoided emissions directly into the mean-variance objective function, rather than as a corrected constraint metric — compared against the paper's own approach
